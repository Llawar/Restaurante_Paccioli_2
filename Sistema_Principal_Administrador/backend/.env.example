PORT=3006
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=restaurant_system_db
JWT_SECRET=restaurant_jwt_secret_key_2024
JWT_EXPIRES_IN=24h
NODE_ENV=development

# Supabase (puente delivery -> POS). La URL es pública, la SERVICE_ROLE_KEY NO.
# Obtenla en: https://supabase.com/dashboard -> Settings -> API Keys -> service_role
SUPABASE_URL=https://oywjtoventqgzcotqpny.supabase.co
SUPABASE_SERVICE_ROLE_KEY=tu_service_role_key_aqui

# URL pública del backend (para construir las URLs de imagen del catálogo en la app).
# Debe ser accesible desde donde corra la app (LAN o pública). Cambia el IP según el entorno.
PUBLIC_BASE_URL=http://192.168.1.50:3006

# GastroStock (BD independiente restaurante_inventarios_db)
GASTRO_API_URL=http://localhost:3007/api
GASTRO_SERVICE_TOKEN=
# Si está vacío, se genera un JWT interno con JWT_SECRET para llamar a Gastro
