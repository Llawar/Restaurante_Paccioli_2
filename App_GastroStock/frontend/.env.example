# Configuración del frontend GastroStock
# Copia este archivo como ".env" en esta carpeta (frontend) si tu backend
# no corre en localhost:3007. Si usas localhost:3007, NO necesitas crearlo
# (el frontend ya lo toma por defecto).

VITE_API_URL=http://localhost:3007/api
